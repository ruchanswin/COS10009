
def sort(hash)
  sorted = {}
  hash.keys.sort.each do |key|
    sorted[key] = hash[key]
  end
end

drivers = {
  4 => 'Alonso',
  1 => 'Vettel',
  3 => 'Raikonnen',
  2 => 'Ricciardo'
}

puts sort(drivers)
